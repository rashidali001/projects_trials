#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, c;

    for (a = 0; a <= 9; a++)
    {
        for (b = 0; b <= 9; b++)
        {
            if (a != b && a<=b)
            {
                for(c = 0; c <= 9; c++)
                    if ( (c != a && c != b) ||
            }


        }
    }
    return 0;
}
